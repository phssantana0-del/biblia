# Gênesis 18 — Revisão

## Pontos de revisão

1. **v2** — OCR exibia `prostrado` com nota de rodapé sobre `adorat`. Transcrição fiel ao texto impresso.
2. **v10** — OCR exibia `d.um`; corrigido para `d'um`.
3. **v11** — OCR exibia `cle ordinário`; corrigido para `de ordinário`. OCR exibia `pensão.`; removido ponto espúrio.
4. **v20** — OCR exibia `So-d.oma`; corrigido para `Sodoma`.
5. **v23** — OCR exibia `chegando-sc.`; corrigido para `chegando-se`.
6. **v33** — OCR exibia `d.epois`; corrigido para `depois`.

## Status
Transcrição concluída. 33 versículos, 4 notas de rodapé.
