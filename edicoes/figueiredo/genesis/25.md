# Gênesis 25 — Revisão

## Pontos de revisão

1. **v5** — OCR exibia `beiis`; corrigido para `bens`.
2. **v6** — OCR exibia `suâs`; corrigido para `suas`.
3. **v18** — OCR exibia `d,esde` e `<1 ue`; corrigido para `desde` e `que`.
4. **v23** — ⚠️ **ATENÇÃO**: OCR exibia `vencerá` nas duas cláusulas. A segunda foi transcrita como `servirá` (conforme Vulgata: *et major serviet minori*). Verificar no PDF original.

## Status
Transcrição concluída. 34 versículos, 3 notas de rodapé.
